<#
  migrate-to-mssql.ps1 — disabled
  The PowerShell migration script has been neutralized to undo SQL-related changes.
  If you need to run migrations in future, re-create or restore the original scripts from version control.
#>
Write-Host "migrate-to-mssql.ps1 is disabled."
